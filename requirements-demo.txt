# Slim runtime deps for the public demo image (artifact-free sklearn router).
# Keep torch/transformers out — they pull large wheels and are optional backends.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv
groq
openai
numpy
scikit-learn>=1.4
pydantic
httpx
